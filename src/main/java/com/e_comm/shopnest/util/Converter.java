package com.e_comm.shopnest.util;

import com.e_comm.shopnest.dto.CategoryDTO;
import com.e_comm.shopnest.entity.Category;

public class Converter {

   /* public static CategoryDTO convertCategoryToCategoryDTO(Category category){
        *//*CategoryDTO categoryDTO = new CategoryDTO();
        categoryDTO.setId(category.getId() != null ? category.getId() : null);
        categoryDTO.setCategoryId(category.getCategoryId() != null ? category.getCategoryId() : null);
        categoryDTO.setCategoryName();
        categoryDTO.setChildId();
        categoryDTO.setParentId();
        categoryDTO.setChildren(null);
*//*
        //return categoryDTO;
    }*/
}
