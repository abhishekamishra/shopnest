spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shopnest
    username: root
    password: MySql@2024

  jpa:
    hibernate:
      ddl-auto: update
      generate-ddl: true
    show-sql: false

server:
  port: 8080